# 🎯 快速开始：跨设备通信

## 💡 好消息

你的系统**已经完整实现**了跨设备、跨桌子通信功能！

- ✅ Table 1 和 Table 2 支持
- ✅ 跨表消息发送
- ✅ 实时 WebSocket 推送
- ✅ 服务器架构完整

## 🚀 5分钟快速测试

### 准备工作
- 2 台电脑（或 1 台电脑 + 1 部手机）
- 连接同一个 WiFi

---

### 在主电脑（服务器）上

#### 1. 启动服务
双击运行：
```
start_lan_mode.bat
```

会自动：
- ✅ 检测你的 IP 地址（例如：192.168.1.100）
- ✅ 启动后端服务器
- ✅ 启动前端
- ✅ 打开浏览器

#### 2. 加入 Table 1
- 选择座位
- 设置头像和状态
- 创建信号

---

### 在第二台电脑上

#### 1. 克隆代码
```bash
git clone https://github.com/heiloMeow/Happy-Graduation.git
cd Happy-Graduation
```

#### 2. 配置 API 地址
```bash
cd frontend

# 创建 .env 文件
# 将 192.168.1.100 替换为主电脑的 IP 地址
echo VITE_API_BASE=http://192.168.1.100:8000/api > .env
```

**如何获取主电脑 IP？**
- Windows：运行 `ipconfig`，查看 "IPv4 地址"
- 主电脑运行 `start_lan_mode.bat` 时会自动显示

#### 3. 启动前端
```bash
npm install  # 仅第一次需要
npm run dev
```

#### 4. 加入 Table 2
- 访问 http://localhost:5173
- 选择座位
- 设置头像和状态
- 创建信号

---

### 测试跨桌通信

#### 从 Table 1 发送消息到 Table 2

1. **在主电脑（Table 1）**：
   - 点击 "Nearby" 查看附近桌位
   - 看到 Table 2 的成员
   - 选择一个成员
   - 发送消息（例如："Can I borrow a charger?"）

2. **在第二台电脑（Table 2）**：
   - 自动弹出消息通知
   - 看到来自 Table 1 的消息
   - 选择回复：
     - 🔴 SORRY（不好意思）
     - ⚪ IGNORE（忽略）
     - 🟢 SURE（当然可以）

3. **在主电脑（Table 1）**：
   - 收到 Table 2 的回复
   - 实时显示对方态度

成功！🎉

---

## 📱 在手机上测试

1. 确保手机连接同一个 WiFi
2. 在手机浏览器打开：
   ```
   http://192.168.1.100:5173
   ```
   （替换为主电脑的 IP）
3. 加入 Table 2
4. 发送/接收消息

---

## ⚠️ 常见问题

### 问题 1：无法连接到服务器

**症状**：
- 第二台电脑显示"网络错误"
- API 请求失败

**解决方法**：

1. **检查 IP 地址是否正确**
   ```bash
   # 在主电脑运行
   ipconfig
   ```
   确认 IPv4 地址

2. **检查防火墙**
   - Windows：控制面板 → 防火墙 → 允许应用
   - 添加端口 8000

3. **测试连接**
   ```bash
   # 在第二台电脑运行
   curl http://192.168.1.100:8000/health
   
   # 应该返回：
   # {"status":"healthy"}
   ```

### 问题 2：WebSocket 连接失败

**症状**：
- 消息无法实时推送
- 控制台显示 WebSocket 错误

**解决方法**：
- 检查 `.env` 文件配置
- 确认 API_BASE 设置正确
- 刷新页面重新连接

### 问题 3：找不到对方的桌子

**症状**：
- "Nearby" 页面没有显示其他桌子
- 只看到自己的桌子

**解决方法**：
- 确认两台电脑都连接到**同一个后端服务器**
- 检查第二台电脑的 `.env` 文件
- 两台电脑都刷新页面

---

## 🔧 高级配置

### 方案 1：内网穿透（公网访问）

使用 ngrok 让任何地点都能访问：

```bash
# 1. 下载 ngrok: https://ngrok.com/download

# 2. 启动后端
start_backend.bat

# 3. 启动 ngrok
ngrok http 8000

# 会显示类似：
# Forwarding: https://abcd1234.ngrok.io -> http://localhost:8000
```

在任何电脑的 `frontend/.env`：
```
VITE_API_BASE=https://abcd1234.ngrok.io/api
```

现在全世界任何地方都能访问！

### 方案 2：云服务器部署

参考 `CROSS_DEVICE_SETUP.md` 获取完整部署指南。

---

## 📊 功能验证清单

测试完成后检查：

- [ ] 两台设备同时在线
- [ ] Table 1 和 Table 2 各有成员
- [ ] 可以看到对方的头像和状态
- [ ] 可以发送消息
- [ ] 可以接收消息并回复
- [ ] 发送方收到回执
- [ ] 实时状态更新（有人加入/离开）

全部勾选 = 完美！🎉

---

## 🎓 工作原理

你的系统是**客户端-服务器架构**：

```
[电脑 1 - Table 1]  ←→  [后端服务器]  ←→  [电脑 2 - Table 2]
    浏览器            FastAPI + DB         浏览器
                      SQLite数据库
                      WebSocket推送
```

**优点**：
- ✅ 数据集中存储
- ✅ 实时同步
- ✅ 支持任意数量设备
- ✅ 无需P2P配置

**特点**：
- 后端必须一直运行（在主电脑上）
- 其他设备通过 IP 地址访问后端
- 使用 WebSocket 实现实时推送

---

## 🚦 下一步

### 立即测试
1. 运行 `start_lan_mode.bat`
2. 在第二台电脑配置并启动
3. 发送第一条跨桌消息！

### 扩展功能
- 添加 Table 3、Table 4...
- 自定义消息模板
- 群发消息
- 消息历史记录

### 正式部署
- 部署到云服务器
- 配置域名
- 启用 HTTPS

---

## 💬 需要帮助？

参考文档：
- 详细配置：`CROSS_DEVICE_SETUP.md`
- 功能说明：`README.md`
- 测试指南：`TESTING_GUIDE.md`

祝测试顺利！🎉

