# 第二台电脑配置指南

## 📋 前置要求

- ✅ 已安装 Node.js（版本 16.x 或更高）
- ✅ 已安装 Git
- ✅ 连接到与主电脑相同的 WiFi 网络

---

## 🚀 方法一：使用自动配置脚本（推荐）

### 步骤 1：克隆代码
```bash
# 直接克隆 Yueteng_Ma 分支
git clone -b Yueteng_Ma https://github.com/heiloMeow/Happy-Graduation.git
cd Happy-Graduation
```

### 步骤 2：运行配置脚本
```bash
# 双击运行
setup_client.bat

# 或在命令行运行
.\setup_client.bat
```

脚本会提示你输入主电脑的 IP 地址（例如：192.168.1.100），然后自动：
- ✅ 安装依赖
- ✅ 配置连接
- ✅ 测试连接
- ✅ 启动前端

完成！

---

## 🔧 方法二：手动配置

### 步骤 1：克隆代码
```bash
# 克隆并切换到 Yueteng_Ma 分支
git clone https://github.com/heiloMeow/Happy-Graduation.git
cd Happy-Graduation
git checkout Yueteng_Ma

# 或者直接克隆指定分支（推荐）
git clone -b Yueteng_Ma https://github.com/heiloMeow/Happy-Graduation.git
cd Happy-Graduation
```

### 步骤 2：安装依赖
```bash
cd frontend
npm install
```

### 步骤 3：配置后端地址
```bash
# 将 192.168.1.100 替换为主电脑的 IP 地址
echo VITE_API_BASE=http://192.168.1.100:8000/api > .env
```

**或手动创建文件**：
- 在 `frontend` 目录下创建 `.env` 文件
- 内容：
  ```
  VITE_API_BASE=http://192.168.1.100:8000/api
  ```

### 步骤 4：启动前端
```bash
npm run dev
```

### 步骤 5：访问应用
打开浏览器访问：http://localhost:5173

---

## 📝 获取主电脑 IP 地址

### 在主电脑上执行：

**Windows**：
```bash
ipconfig
```
查找 "IPv4 地址"，例如：`192.168.1.100`

**Mac/Linux**：
```bash
ifconfig | grep inet
```

---

## ✅ 验证连接

### 测试后端连接：
```bash
# 将 IP 替换为主电脑的地址
curl http://192.168.1.100:8000/health
```

**预期返回**：
```json
{"status":"healthy"}
```

如果返回这个结果，说明连接成功！

---

## 🎯 完整流程示例

假设主电脑 IP 是 `192.168.1.100`：

```bash
# 1. 克隆代码
git clone -b Yueteng_Ma https://github.com/heiloMeow/Happy-Graduation.git
cd Happy-Graduation

# 2. 配置前端
cd frontend
npm install

# 3. 创建配置文件
echo VITE_API_BASE=http://192.168.1.100:8000/api > .env

# 4. 测试连接
curl http://192.168.1.100:8000/health

# 5. 启动前端
npm run dev

# 6. 打开浏览器
# 访问 http://localhost:5173
```

---

## ⚠️ 常见问题

### 问题 1：无法克隆代码

**错误**：`fatal: could not read Username for 'https://github.com'`

**解决**：仓库可能是私有的，需要：
1. 确认你有权限访问该仓库
2. 或者使用 SSH 方式克隆
3. 或者直接从主电脑复制代码文件夹

### 问题 2：npm install 失败

**错误**：`npm: command not found`

**解决**：
1. 安装 Node.js：https://nodejs.org/
2. 重启终端
3. 验证：`node --version`

### 问题 3：无法连接到服务器

**错误**：API 请求失败，控制台报错

**检查清单**：
- [ ] 主电脑的后端正在运行
- [ ] IP 地址是否正确
- [ ] 两台电脑在同一 WiFi
- [ ] 防火墙是否开放 8000 端口

**测试连接**：
```bash
# 替换为实际 IP
ping 192.168.1.100
curl http://192.168.1.100:8000/health
```

### 问题 4：端口被占用

**错误**：`Port 5173 is already in use`

**解决**：
```bash
# 方法 1：关闭占用端口的程序
# 方法 2：指定其他端口
npm run dev -- --port 5174
```

---

## 📊 配置检查清单

安装好后，验证以下内容：

- [ ] 代码已从 Yueteng_Ma 分支克隆
- [ ] `frontend/node_modules` 目录存在
- [ ] `frontend/.env` 文件存在且配置正确
- [ ] 能够访问 `http://localhost:5173`
- [ ] 能够登录并加入 Table 2
- [ ] 能看到 Table 1 的成员（在 Nearby 页面）
- [ ] 能发送和接收消息

---

## 🎓 工作原理

```
┌─────────────────┐         ┌─────────────────┐         ┌─────────────────┐
│   主电脑         │         │   后端服务器     │         │   第二台电脑     │
│   (Table 1)     │◄────────┤   FastAPI       │────────►│   (Table 2)     │
│                 │         │   + SQLite      │         │                 │
│   前端:5173     │         │   + WebSocket   │         │   前端:5173     │
│   后端:8000     │         │   运行在主电脑   │         │   (仅前端)      │
└─────────────────┘         └─────────────────┘         └─────────────────┘
         │                                                        │
         └────────────────── 同一 WiFi ─────────────────────────┘
```

**关键点**：
- 主电脑运行后端和前端
- 第二台电脑只运行前端
- 两个前端都连接到主电脑的后端
- 数据存储在主电脑的数据库中

---

## 💡 快速记忆

**第二台电脑只需要 3 样东西**：
1. ✅ Node.js 环境
2. ✅ 前端代码（从 Yueteng_Ma 分支）
3. ✅ `.env` 配置文件（指向主电脑 IP）

**不需要**：
- ❌ Python
- ❌ 后端代码
- ❌ 数据库

---

## 🚦 下一步

配置完成后：
1. 主电脑加入 Table 1
2. 第二台电脑加入 Table 2
3. 测试跨桌通信功能
4. 享受无缝协作体验！

有问题参考 `CROSS_DEVICE_SETUP.md` 获取更多帮助。

